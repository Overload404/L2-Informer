import Table from "react-bootstrap/Table"

const SpoilTable = (props) => {
  let spoilExists = props.spoil != undefined
  console.log(spoilExists)
    return(
        <div>
          
          </div>
    )
}

export default SpoilTable

